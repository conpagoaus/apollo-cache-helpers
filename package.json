{
  "name": "@conpagoaus/apollo-cache-helpers",
  "version": "0.1.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "tsc",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@apollo/client": "^3.7.1",
    "typescript": "^4.6.4",
    "vitest": "^0.24.5"
  },
  "peerDependencies": {
    "@apollo/client": "^3.0.0"
  },
  "dependencies": {
    "immer": "^9.0.16"
  }
}
